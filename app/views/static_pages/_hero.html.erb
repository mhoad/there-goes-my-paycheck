<!-- Start featured -->
  <section id="featured" class="aligncenter">
    <div class="gridrotator">
      <div class="grid-wrapper">
        <div class="container">
          <div class="row">
            <div class="span12">
              <h3>FMH Gifts</h3>
              <p>Say Goodbye To The Next Hour of Your Life</p>
            </div>
          </div>
        </div>
      </div>
      <div id="ri-grid" class="ri-grid ri-grid-size">
        <ul>
          <% 63.times do %>
            <% offset = rand(1..Product.count) %>
            <% random_product = Product.limit(1).offset(offset) %>
              <li><a href="#">
                <% if !random_product.first.nil? %>
                  <%= image_tag(random_product.first.picture(:thumbnail)) %>
                <% else %>
                  <img src="http://placehold.it/1024x680.jpg" alt="" >
                <% end %>
              </a></li>
          <% end %>
        </ul>
      </div>
    </div>
  </section>
<!-- End featured -->
